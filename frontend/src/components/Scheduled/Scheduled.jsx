import { Box } from "@mui/material";
import React from "react";

function Scheduled() {
  return (
    <Box
      sx={{
        display: "flex",
        flex: "1",
        width: "100%",
        backgroundColor: "blue",
        borderRadius: "10px",
      }}
    ></Box>
  );
}

export default Scheduled;
